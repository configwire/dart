# Learnings — confignest

Conventions, patterns, and successful approaches discovered during work on this plan.

_Auto-scaffolded by /ulw-execute. Append new entries below - never overwrite._

---

## 2026-09-22 execution-start seed (from planning research)
- PocketBase v0.40.4 Go idioms: migrations pkg with m.Register(up,down) + migratecmd.MustRegister Automigrate; routes inside OnServe BindFunc via se.Router.GET/POST ({param} patterns), must return se.Next(); static via apis.Static(os.DirFS("./pb_public")) mounted LAST on /{path...} or it swallows /api/*.
- DB discipline: use e.App/re.App inside handlers, never captured app (deadlock risk). Go hooks only, no JSVM split.
- ENV FACT: project dir is NOT a git repo -> no worktrees, no git commits; workers skip Commit steps, keep working-tree changes. Installed go1.25.1 vs server/go.mod `go 1.27` -> watch GOTOOLCHAIN download on first build. Dart 3.13.3 satisfies ^3.4 floor.
- Spike QA discipline that worked (todo 2): PID-tracked `serve --dir=/tmp/...` + curl assertions with `set -u -o pipefail`, tee to `.omo/evidence/` at PROJECT root (watch cwd — server/ has no .omo tree; move the log if mis-placed). PB error JSON uses `{"data","message","status"}` (no `code` key) — assert on `status`. Unauthenticated `POST /api/realtime` subscribe to an arbitrary topic returns 204 (length validation only); per-topic auth is enforced at delivery via collection rules, and the broker exposes per-client `Send`, not topic publish.
- PB v0.40.4 handler facts (no new deps needed): path params via `re.Request.PathValue("env")` with `{env}` route patterns; `re.JSON/re.String` for bodies; return `re.UnauthorizedError/re.NotFoundError/re.BadRequestError(msg, nil)` as errors (never 500 on the spike paths); `re.App.SubscriptionsBroker()` + `tools/subscriptions.Message{Name, Data}` for broker access; SSE = set `Content-Type: text/event-stream` + `fmt.Fprintf(re.Response, ...)` + `re.Flush()`.

## 2026-09-22 task-4 (Dart scaffold)
- Dart SDK observed: 3.13.3 stable on macos_arm64. Resolved: http 1.6.0, meta 1.19.0, test 1.32.0, lints 5.1.1 (lints 6.1.0 available but outside ^5.0.0).
- DECISION: dropped `shared_preferences` from pubspec — it is Flutter-only (pulls flutter SDK pkgs into `dart pub deps`), violating the pure-Dart rule. Scaffold ships http+meta only; todo 12 picks a pure-Dart cache for {etag,version,fetchedAt,values}.
- macOS has no GNU `timeout` binary (`command -v timeout gtimeout` empty) — hung-command bound N/A; `dart pub get` completed online (Got dependencies, exit 0).
- Analyzer-proof: `dart analyze` exits 2 on unused-import probe, 0 clean — analyzer active, no fake green. Evidence: .omo/evidence/task-4-confignest.log.

## 2026-09-22 task-1 rename myapp->confignest (worker T1, port 8091)
- GOTOOLCHAIN verdict: installed go1.25.1, go.mod says `go 1.27`, GOTOOLCHAIN=auto — NO download happened; `go mod tidy` + `go build ./...` + `go vet ./...` all exit 0 on local toolchain. go directive left untouched at 1.27.
- `go run . serve` orphans the compiled child (`.../exe/myapp serve`, PPID 1) when the wrapper exits; must hunt the child via `lsof -ti:PORT`, not just the recorded wrapper PID. Directly-run binaries may be reaped between tool calls — curl exit code is the authority for up/down, not PID bookkeeping.
- lsof noise: Arc Browser Helper transient sockets show up on `lsof -ti:8091`; verify identity with `ps -o command` before killing.
- PB first boot on fresh dir takes ~10s (migrations + superuser installer print); allow sleep >=10s before curl, else false 000.
- Baseline vs after identical: `Hello world!` HTTP 200 both times; only go.mod line 1 + go.sum touched, zero .go changes.

## 2026-09-22 task-3 evaluator vectors tests-first (worker T3)
- CONTRACT todo 6 implements (frozen, see server/eval/eval.go header): `Evaluate(flag Flag, rules []Rule, ctx Context) any` pure (no DB); first-true rule wins (conditions AND-ed), rule-value/flag-type mismatch skips rule, else flag.Default; `Assign(exp Experiment, userID string) string` sticky-by-construction, empty user/variants -> DefaultVariant; `Bucket(userID, seed string) int` = int(fnv64a(userID+seed)%10000) in 0..9999, -1 when either empty. Condition schema: platform(==/!=/contains/regex, case-sensitive exact), appVersion(</<=/==/!=/>=/> strict X.Y.Z+opt suffix, invalid either side -> false; contains/regex raw), locale/country(== exact, no implicit prefix), percentile(<= inclusive / between [lo,hi] inclusive, condition seed else ctx.PercentileSeed, missing -> false), custom.<name> flat map strict-typed (missing/mismatch -> false). json flags accept object/array only (scalars fall through). Never panic, never error: all invalid -> condition false -> default.
- Vectors: server/eval/vectors_test.go TestVectors, 46 table cases + determinism_10k + sticky_fixture + type_matrix + broken_fixture subtests; fixtures in server/eval/fixtures/ (4 flags, ordered rules, android ctx, experiment_sticky 10 users, broken_condition). RED as designed: 27 fail TODO(todo6), 19 pass (fallthrough/sticky), 0 panics, suite exit 1. Determinism -count=3 byte-identical (fixed=-1 lo=10000 each). Evidence: .omo/evidence/task-3-confignest.log.
- GOTCHA fixed: placeholder inside JSON string (`"value [$X]"`) breaks decode -> keep placeholder as the whole JSON value (`"value":$X`, substituted with `[b,b]`).
- gopls LSP unavailable (user declined install); used `go vet` (exit 0) + `gofmt -l` (clean) instead. No Context7 call: semver/percentile semantics settled from plan + Firebase notes already in repo.

## 2026-09-22 Wave-2 closeout (orchestrator-verified)
- PB v0.40.4 rule semantics: nil rule = deny (403 "Only superusers..."), "" (empty non-nil) = PUBLIC. All 9 collections ship nil rules. Superuser bypasses RULES but hooks (OnRecordValidate/Update) still fire — so releases immutability + flag-key hooks hold on every path including dashboard. Later todos: never rely on rules alone for invariants; use hooks.
- migrate down prompts for confirmation (pipe `y` in scripts). No `migrate history` subcommand. Automigrate:true wired in main.go.
- Evaluator: hand-rolled stdlib semver (no dep), Bucket/Assign per contract, zero-condition rules vacuously match (documented, no vector — acceptable).
- Migration file header comment line 10 says default-deny ("") — STALE phrasing (code correctly uses nil); T8 worker: fix that comment line when touching main.go/migrations area.

## 2026-09-22 task-6 evaluator implementation (worker T6)
- SEMVER CHOICE: hand-rolled strict parser in stdlib, NO new dependency (go.mod/go.sum untouched). Rationale: frozen vectors need only strict MAJOR.MINOR.PATCH + optional -prerelease/+build (numeric cmp, prerelease<release, build ignored, unparseable either side -> false); a third-party lib adds supply-chain surface for zero behavioral gain. Choice documented in eval.go comment on compareSemver. Verified prerelease `1.0.0-alpha < 1.0.0` true, build `1.0.0+b1 == 1.0.0` true, 200-char version -> false no-panic.
- BUCKET VERIFIED: fnv64a(userID+seed)%10000, -1 on empty either side; unicode userID `üser-🎉-日本語` -> stable 1637 in 0..9999; determinism_10k lo=4964/10000 (within 5000±250), -count=3 byte-identical.
- COERCION notes: number accepts any Go numeric incl. json.Number (float64 200/19.99 from vectors); string/bool exact-type only; json accepts string-keyed maps (reflect-generalized) + slices/arrays except string/[]byte; unknown flag type rejects. Rule with zero conditions vacuously matches (no vector covers it).
- QA: `go test ./eval/ -v -count=1` 50/50 PASS (46 table + determinism + sticky + type_matrix + broken), `go test ./...` exit 0, `go vet ./...` exit 0, tree-wide `gofmt -l` empty (applied allowed formatting-only fix to pre-existing space-indented server/main.go, verified whitespace-only via `gofmt -d` eyeball). Fuzz spots all defaults/no-panic (temp probe file removed after run). Evidence: .omo/evidence/task-6-confignest.log. RISKS: none — no vector looked wrong; strict leading-zero rejection (`01.2.3` invalid) goes beyond vectors but matches semver §2.

## 2026-09-22 task-5 (PocketBase migrations, port 8094)
- SECURITY GOTCHA (v0.40.4, apis/record_crud.go:52,165,231,420,557): nil rule = deny non-superusers (403 "Only superusers"), but "" (non-nil empty) applies NO filter and leaves list/create/view/update/delete PUBLIC. Default-deny MUST be nil, not "". First QA run had "" rules -> unauth list returned 200 on all 9; fixed to nil -> 403 everywhere. Evidence: .omo/evidence/task-5-confignest.log (PASS=40 FAIL=0).
- `migrate down N` ALWAYS prompts YesNoPrompt (core/migrations_runner.go:83); non-interactive use requires `echo y |`. `migrate collections` also prompts (snapshot generator) — not needed for verification; `migrate up` + sqlite3 .tables is the check. There is no `migrate history` subcommand (unsupported command).
- Hooks: app.OnRecordUpdate("releases") + app.OnRecordValidate("releases") with IsNew() guard deny all updates (API PATCH -> 400 "Failed to update record", snapshot byte-identical after). Flag key hook on create+update: regex ^[A-Za-z_][A-Za-z0-9_]*$, 1..128 chars (TextField Pattern+Max mirror it); 1000/project cap via FindAllRecords+in-Go filter to avoid a direct dbx dep (go.mod owned by T6). Bad key `1bad` and 129-char key both -> 400 live.
- Migration file server/migrations/1787875200_init_confignest.go (m.Register, ids cn_*), down deletes 9 in reverse-dep order (idempotent skip). main.go adds migratecmd.MustRegister Automigrate + blank import confignest/migrations + registerConfignestHooks; gofmt clean, build/vet green, go.mod/go.sum untouched.

## 2026-09-22 task-7 experiments overlay + exposure builder (worker T7, eval-only)
- OVERLAY SEMANTICS: EvaluateWithExperiment(flag,rules,ctx,exp,status) in server/eval/experiment.go. status!=running (draft/stopped/unknown/empty) -> control path (Evaluate base, variant=DefaultVariant, no overlay read). running -> variant=Assign(exp,ctx.UserID), then variant.Values[flag.Key] present -> coerce to flag.Type via existing coerce (success=override, wrong-type=base fallthrough); absent -> base unchanged. Never errors/panics: bad weights/empty variants fall back to control.
- VARIANT VALUES SHAPE: additive `Values map[string]any json:"values,omitempty"` on Variant in eval.go; Assign/Bucket/Evaluate ignore it (frozen behavior intact, vectors still green). Weights WeightBps sum MUST be exactly 10000: ValidateExperiment rejects !=10000 (9900 test) and empty/negative with error, no renormalize.
- EXPOSURE HASHING: HashUserID=hex(sha256(userID))[:16], empty->"". Exposure struct frozen JSON {env,flagKey,variant,userHash,timestamp} — no userId/ip/raw fields; this is T9 ingest's input contract. Test asserts marshalled JSON has zero userId/ip keys and stored != raw for test-user-042.
- QA: split 10k seed exp-seed-1 -> control:4990/treatment:5010 (within 4800..5200), 3x -count=1 byte-identical; sticky 1k 100% identical; go test ./... exit 0, vet exit 0, gofmt clean. Evidence: .omo/evidence/task-7-confignest.log (project root; server/.omo removed after move).

## 2026-09-22 task-9 ingest write path (port 8096)
- ROUTE+FILES: `POST /api/v1/env/{env}/events` in new package server/ingest/ (ingest.go validation+auth, handler.go route+limiter+wiring, batcher.go async writer, hash.go, ingest_test.go 16 tests green). main.go diff is 3 lines: import + ingest.EnableWAL(app) + ingest.Register(se). No migration edits, no new deps, no eval/spike/client changes.
- KEY FORMAT (T10/T11/T16 contract): full opaque key in `X-ConfigNest-Key`; sdk_keys stores prefix=first-8-chars (prefilter) + hash=hex(sha256(fullKey)) (constant-time compare). Full key never stored. Unknown/missing/revoked/env-mismatched -> 401. Exported helper `ingest.RequireSDKKey(re)` returns the key record (T10: reuse it for fetch/stream auth). O(n) FindAllRecords scan documented — fine at tens of keys.
- RATE (fixed-window, per key-hash): sdk_keys.rateLimit req/60s window, default 60 when missing/non-positive; over -> 429. Every authed hit consumes a token (even later-400s). Live: 70 rapid singles on 60-limit key -> 51x202 + 19x429. Memory: one map entry per key-hash, sweep of >2-window-idle entries at most 1/s — key-scan growth risk noted (keys are server-issued, accepted).
- FLUSH BOUNDS: channel cap 2048, flush every 1s or 500 rows via app.Save; worst-case lag ~1s+write (measured: 100-batch 202 -> 100 rows visible after sleep 2). Full buffer -> 503 (non-blocking; never silent-drop, never block handler — documented in batcher.go). Shutdown: OnTerminate drains best-effort + log receipt ("batcher stopped, drained N").
- BODY CONTRACT: {events:[{kind fetch|exposure, flag, variant, userHash?, ts?}]}; 1..100 events (>100 -> 400); raw userId/ip keys (even null) -> 400 strict reject; bogus kind/empty/non-JSON/bad-ts -> 400; single event >64KB or body >128KB -> 413. ts = RFC3339 string | unix-seconds number | absent=server time. Unknown env slug -> 404. Unknown flag keys stored with relation unset (variant preserved) — schema has no flagKey text field.
- T7 ALIGNMENT: T7 landed server/eval/experiment.go mid-task (Exposure{env,flagKey,variant,userHash,timestamp}, HashUserID=hex(sha256)[:16], empty->""). ingest.HashUser matches exactly incl. empty->"" (aligned post-QA, tests re-green). Field-name mapping T7-flagKey->ingest-flag, T7-timestamp->ingest-ts is the SDK/client (T12/T14) mapping job; server accepts its own names.
- WAL GOTCHA (crash-grade): e.App.DB() is NIL inside OnBootstrap (panic SIGSEGV at boot) — WAL pragma must bind OnServe where the store is open. Verified `sqlite3 data.db "pragma journal_mode;"` -> wal in live QA. Evidence: .omo/evidence/task-9-confignest.log (PASS=21 FAIL=0). QA seed: superuser upsert + login, project/env dev, sdk_keys row via API, flag hero_button. RISKS: ingest lag only measured at ≤2s granularity (no p99); limiter map unshared across restarts (window resets on reboot); flag-resolution does 1 query per distinct flag key per request.

## 2026-09-22 task-8 releases API (port 8098) — T10/T13/T17 CONTRACT (frozen)
- FROZEN snapshot schema (no deviations): `{"flags":[{"key","type","default","group","rules":[{"priority","condition","value}]}],"experiments":[{"id","flag","seed","variants","status"}]}`. flags/rules scoped to env's project (flags sorted by key, rules by priority asc for deterministic bytes); experiments included as-is (ALL rows, no env filter — migration has no env/project field on experiments). experiment.flag = linked flag KEY ("" if unset), experiment.id = record id, flag.group = groups record id ("" if unset).
- ETAG RULE: `etag = hex(sha256(version+":"+snapshot))[:16]` over canonical server-marshaled bytes. Version is in the hash input -> rollback rows get a FRESH etag despite byte-identical snapshots. Verified live: v1 etag 5dadac695eb4c603 vs v3 (rollback of v1) 1f75363af9defec2, sqlite bytes v1==v3.
- 409 SEMANTICS: publish body {note, baseVersion}; baseVersion must equal env's current max (0 on first publish). Mismatch -> 409 `{"message","status":409,"currentVersion":N}` with NO write (row count proven unchanged). Rollback `POST /api/v1/admin/releases/:version/rollback {note?}` copies source snapshot bytes verbatim into NEW row version=max+1 in the SOURCE row's env; unknown version -> 404; version shared by 2+ envs -> 400 ambiguous (single-env QA unaffected).
- SERVER-SIDE BUILD ONLY (deviation rationale): snapshot assembled from live collections inside the handler, never client-supplied — reuses key-shape/cap/type checks on a dry assemble (no writes except the release row). Publish validation: non-empty ("nothing to publish" 400 on flag-less env), key regex + 128 chars, 1000/project cap, flag type in number|string|bool|json with default AND rule values coerced to flag type (bool flag + string rule value -> 400), every experiment via eval.ValidateExperiment (9900-bps weights -> 400). Bad key `1bad` cannot be seeded via API (hooks forbid) -> covered by unit test.
- AUTH: both routes `.Bind(apis.RequireSuperuserAuth())`; SDK-key-only and unauth -> 401 (verified live). Author = superuser email (id fallback). Audit = std `log.Printf` lines (`releases: published/rolled back env version etag author note`) — PB `App.Logger().Info` is SILENT by default (live gotcha), so std log is required for audit visibility. No new collection.
- CONCURRENCY: process-local writeMu serializes publish+rollback version assignment -> concurrent double-publish {baseVersion:N} yields exactly one 200 + one 409, +1 row, zero duplicate versions (verified live twice). Cross-process serialization out of scope (single binary).
- GOTCHAS: BindBody returns nil on empty body (ContentLength==0) -> explicit empty-body 400 check required; baseVersion-as-string fails decode -> 400. JSON field Set with canonical string stores bytes verbatim (field_json.go PrepareValue); rollback copies via GetRaw+String() -> byte-identical DB rows. `re.Auth *core.Record` holds the superuser (middlewares.go loadAuthToken, `Authorization: TOKEN`, Bearer prefix optional). Evidence: .omo/evidence/task-8-confignest.log (30 PASS + focused race PASS). RISKS: experiments-as-is means one broken experiment blocks ALL envs' publishes (strict fail-fast, documented); multi-env same-version rollback is 400-ambiguous by design (per-env rollback route belongs to a later todo if needed).

## 2026-09-22 task-10 SDK fetch API (port 8099) — T12/T13/T14/T17 CONTRACT (frozen)
- ROUTE+FILES: `GET /api/v1/env/{env}/config` in new package server/fetch/ (fetch.go handler+wiring+pure helpers, fetch_test.go green). main.go diff is 3 lines: import + fetch.Register(se) anchored after releases.Register(se). `.Bind(apis.Gzip())` on the route (live: Content-Encoding gzip present). No migration edits, no new deps, no releases/ingest/eval/migrations/client changes.
- SPIKE-CONFIG REMOVAL: deleted `spikeConfig`+its route line from server/spike_probe.go (real endpoint owns the path now; helper would be dead code). `grep -n spikeConfig server/*.go` -> empty. Spike `/stream` + `/spike/publish` INTACT (live: stream 200 + config_update bytes, required by T14). Removal noted at the call site.
- AUTH ORDER: 401 (ingest.RequireSDKKey reuse: missing/unknown/revoked) -> 404 (unknown env slug) -> 401 (key env-scope mismatch) -> 400/414 (attrs) -> 200/304. Live: nokey/badkey/revoked/env-mismatch all 401; unrevoke restores 200.
- EMPTY-RELEASE POLICY: env with no release row -> 200 `{"version":0,"etag":"none","values":{},"variants":{},"fetchAt"}`. NEVER 404 (fresh envs boot clean); values EMPTY (not flag defaults — nothing published, SDKs use in-app defaults).
- ETAG: stored etag + stored snapshot bytes served VERBATIM (sqlite etag == served etag == ETag header, proven live v1 b41b62605c0df712). Never recomputed. If-None-Match EXACT match only -> 304 empty body (size_download 0); `W/"..."` prefix does NOT match -> 200 (documented).
- EVALUATION: per snapshot flag rebuild eval.Flag/Rules (single stored condition wrapped as one eval.Condition; null/undecodable -> zero conditions = vacuous match) and eval.Evaluate; flags targeted by a running experiment overlaid via eval.EvaluateWithExperiment + variant recorded. variants map has an entry ONLY for flags with >=1 targeting experiment row; first row (snapshot id order) wins. Snapshot carries no defaultVariant -> anonymous/empty-user Assign falls back to "" (recorded as variant "", base value served). Deterministic proof live: offline python fnv1a(userID+seed)%10000 predicted treatment(qa-user-7)/control(qa-user-9), server values+variants matched exactly.
- CTX/ATTRS POLICY: uid->UserID (absent -> anonymous, works); platform/appVersion/locale/country passthrough (appVersion=not-semver -> 200 defaults fallthrough, never 400); attrs must be a JSON object (malformed/array/string/number/null/truncated -> 400) and <=8192 raw bytes (over -> 414). Unit-tested incl. at-bound edge.
- EXP OVERRIDE: ?exp= replaces each experiment's stored status for the overlay (exp=draft forces control path, live-verified); absent -> stored status (only stored-running overlays).
- CORS: Access-Control-Allow-Origin default `*` (live-verified incl. 304); restrict via CONFIGNEST_CORS_ORIGIN=https://app.example.com (single origin, simple header, no infra). OPTIONS preflight route -> 204 with Allow-Methods/Headers + 86400 max-age (live-verified). Never log SDK keys/userIDs (hashes only).
- PERF: 102-flag fetch ~1.0-1.3ms localhost (5 runs, 2.1KB body) -> ~10ms linear extrapolation at 1000 flags; eval cost O(flags+rules)/request, no DB per flag (one releases scan + one env lookup). 1000-flag risk: still single-digit ms, no action needed.
- QA: .omo/evidence/task-10-confignest.log PASS=26 FAIL=0 (200 shape/overlay both arms, 304, weak-no-match, 4x401, 404, empty policy, gzip, not-semver, attrs 400/414, anonymous, hello/stream/events/publish-staging/OPTIONS regressions, migrate up no-op). `go test ./...` green, `go vet` green, `gofmt -l` empty. Cleanup: server DOWN_CONFIRMED, /tmp/cn-t10-* removed, no pb_data in repo.

## 2026-09-22 task-8fix condition-shape hardening (fail-closed publish)
- GAP: ValidateSnapshot checked keys/types/values/experiments but never rule condition shape, while fetch decodeRule maps undecodable/null conditions to ZERO conditions which vacuously MATCH — a corrupt condition would match-all at fetch (fail-open). No behavior break found in live data; fixed at publish time, no revert needed.
- FIX: ValidateSnapshot now runs validateCondition per rule (after the value check): condition must be a JSON object with string field/op + EXISTING value key (any JSON incl. null counts; missing value key -> 400). Field must be platform/appVersion/locale/country/percentile or custom.<non-empty-name> (bare "custom"/"custom."/unknown -> 400). Op allowlists mirror eval.go EXACTLY (platform/locale/country: ==,!=,contains,regex; appVersion: <,<=,==,!=,>=,>,contains,regex; percentile: <=,between; custom.*: ==,!=,<,<=,>,>=,contains,regex) — drift warning comment at the allowlists: adding an eval op requires adding it here. Error shape `flag %q: rule %d has invalid condition: ...` (400 at publish). fetch decodeRule fallback LEFT UNTOUCHED (defense in depth; publish now guarantees shape).
- QA: releases_test.go TestValidateSnapshotAcceptsConditionShapes (7 valid incl. null-value) + TestValidateSnapshotRejectsBadConditions (13 rejects: unknown field/op, bare/empty custom, non-object array/string/number/null, missing value/op/field, non-string op). `go test ./... -count=1` green (all 4 pkgs), `go vet` exit 0, `gofmt -l` empty after whitespace-only var-block alignment. Evidence: .omo/evidence/task-8fix-confignest.log. No temp files created, no other files touched (releases/snapshot.go + releases_test.go only).

## 2026-09-22 task-11 stats query API (port 8101) — T13/T17 CONTRACT (frozen)
- ROUTE+FILES: `GET /api/v1/admin/env/{env}/stats?flag=X&since=7d` in new package server/stats/ (stats.go handler+pure helpers, stats_test.go 5 tests green). main.go diff is 2 lines: import + stats.Register(se) anchored after fetch.Register(se). `.Bind(apis.RequireSuperuserAuth())`; SDK-key-only and unauth -> 401 (live-verified). No migration edits, no new deps, no fetch/ingest/releases/eval/migrations/client changes.
- RESPONSE: `{"fetches":int,"exposures":int,"perVariant":{variant:int},"version":maxReleaseVersion}` aggregated in-Go over FindAllRecords("events") filtered by env/flag/kind/ts (O(n) documented in code + future-index follow-up, v1-appropriate). Counts exact integers, no rounding. perVariant covers EXPOSURES only (verbatim variant names, incl. "" if stored); fetches are flag-filtered too. userHash never read (aggregate counts only, no PII leaves server).
- SINCE GRAMMAR: `?since=<N>d`, absent/blank -> 7d default, N>90 clamped to 90d (never an error), N<1 or wrong shape (abc, -5d, 0d, 7h, bare "7") -> 400. Cutoff = now-UTC minus days; ts BEFORE cutoff or zero-ts excluded.
- ZERO-POLICY: unknown flag keys (incl. adversarial `../x`) -> zeros 200 with version still populated (never 404); unknown env slug -> 404. Events with unset flag relation are excludable: dropped under any ?flag= filter, counted when flag absent.
- QA (route-seeding proves T9→T11 composition, never direct DB writes): 1x100-fetch batch + 1x100-exposure batch (60 control/40 treatment) + 1x100d-old exposure all 202 via T9 route -> stats exact {100,100,{control:60,treatment:40},v1}; since=400d -> same exact (old row excluded); regressions fetch-200/events-202/publish-prod-200 all green AFTER wiring (events regression runs LAST so count assertions stay exact). Evidence: .omo/evidence/task-11-confignest.log (PASS=18 FAIL=0). `go test ./...` all 5 pkgs green, vet green, gofmt clean. Cleanup: server DOWN_CONFIRMED (curl 000), /tmp/cn-t11* removed, no pb_data.
- GOTCHAS: ingest flush is async (~1s) -> sleep >=3s after seeding before asserting counts; data-API superuser auth header is BARE/Bearer token (`Authorization: <token>`), NOT `Authorization: TOKEN <token>` (the T8 notepad phrasing misled once -> 403 "Only superusers", bare token -> 200); `go run . serve` child must be killed via `lsof -ti:PORT` + ps-identity check; seeding needs sdk_keys.rateLimit high (100000 used) to avoid 429 noise.

## 2026-09-22 task-12 Dart offline-first client (port 8102)
- FILES (client/dart/ ONLY): lib/src/cache.dart (File-JSON {etag,version,fetchedAt,values} via dart:io, corrupt->null never throws), lib/src/events.dart (postFetchEvent single {kind:fetch,flag:"",variant:"",userHash} + sha256Hex16), lib/src/config_nest_base.dart (full fetch impl; re-exports cache/events), example/main.dart (CN_LIVE=1 live path, values printed / apiKey never), test/config_nest_test.dart (14 tests).
- userHash CHOICE: `crypto` package (pure-Dart SHA-256, no Flutter/native) over hand-rolling — matches server HashUserID incl. empty->""; cross-checked live: dart sha256Hex16(qa-user-7)=4017c6d850aedf6b == `shasum -a 256` vector.
- THROTTLE: default 12h, zero disables (dev); only 200/304 arm it — errors never block retries (offline devices must retry). Cache-loaded fetchedAt arms throttle (fresh cache = just fetched).
- FETCH-EVENT: single aggregate event (flag "" has no variants entry so variant ""), awaited with 5s timeout inside try/catch — fire-and-forget, never fails fetch; POST body shape asserted in MockClient (kind/flag/userHash); live server-side row NOT asserted (best-effort by contract, batcher lag makes it timing-sensitive).
- VARIANTS: server `""` (anonymous/DefaultVariant fallthrough) OMITTED from getVariant/getVariants (null/absent, never a real arm). Values layering: live view = {...defaults, ...serverValues} so empty-release 200 (values:{}) still serves in-app defaults.
- CONCURRENCY: no lock (single-threaded Dart); overlapping fetchAndActivate x2 both run, last-completes-wins, never torn — test asserts [true,true] + consistent values. Callers needing serialization must await.
- RISK (recorded): default cacheFile `.config_nest_<env>-cache.json` is CWD-relative — pass an explicit app-documents path in production (example takes CN_CACHE).
- QA: `dart analyze` 0 issues; `dart test` 14/14 green (200-update incl. 4-key cache file assert, 304-keep, offline-cache, cold-defaults, type-mismatch, throttle-skip, zero-no-throttle, malformed-200, 500, timeout-hang, concurrent-x2, anonymous-variant-omit, uid+userHash); `dart pub deps` zero flutter/shared_preferences lines. Live (8102, /tmp/cn-t12-pbdata): seed project/env/bool-flag+platform-rule/sdk-key/publish v1 (etag d6f5d92688fa951a) -> example prints launch_flag=true status=success version=1; kill server -> cached launch_flag=true status=error (offline proof); rm cache -> defaults launch_flag=false status=error (cold proof). Evidence: .omo/evidence/task-12-confignest.log (PASS=19 FAIL=0 + receipts). Cleanup: DOWN_CONFIRMED (lsof empty), all /tmp/cn-t12* removed, no pb_data.
- GOTCHA: curl-down check `curl -w 000 || echo 000` concatenates to `000000` on refusal — compare against `*000*` or use lsof as authority (T1 rule restated).

## 2026-09-22 task-13 admin UI shell (port 8103) — T17/T18 CONTRACT
- FILES: server/pb_public/{index.html (login form + nav + #flag-table + #group-filter + #release-list w/ per-row rollback buttons + #publish-form note+baseVersion + #stats-view), app.js (memory-first token, htmx:configRequest bare-header injection, render helpers, window.cnAdmin), styles.css (tokens, no framework), vendor/htmx.min.js PINNED htmx 2.0.7 sha256 60231ae6ba9db3825eb15a261122d5f55921c4d53b66bf637dc18b4ee27c79f9 (unpkg; CDN script tag documented as fallback inside index.html comment)}. main.go diff: +os/+apis imports, `se.Router.GET("/{path...}", apis.Static(os.DirFS("./pb_public"), true))` LAST after spike routes. No Go logic touched (go test ./... green all pkgs).
- TOKEN RULE (refined): bare `Authorization: <token>` always works (200); `TOKEN <token>` prefix NEVER works — 401 on RequireSuperuserAuth custom routes (publish/stats/rollback), 403 on data-API collections (T11). app.js sends bare everywhere incl. htmx partials. Token memory-first, localStorage copy for reload convenience — XSS NOTE: stored token readable by injected scripts; logout clears both; shared machines should skip persistence.
- CWD-SENSITIVITY (T18 read this): serve MUST run from server/ — binary started from repo root answers `/` 404 File-not-found JSON (verified port 8104 probe) while /hello stays 200. No root README exists; ownership limited new files to pb_public, so this notepad entry IS the pinned cwd note.
- JS CONTRACT shapes (curl-proven identical): publish {note, baseVersion} (baseVersion pre-read from stats version -> no first-try 409; forced stale baseVersion=1 after v2 -> 409 {currentVersion:2}); rollback {note} (v1->v2 new row); stats GET ?flag=&since= {fetches:1,exposures:1,perVariant:{control:1},version}. flags render uses `defaultValue` field name (NOT `default`). Release list via data API /api/collections/releases/records?sort=-version (superuser-gated). Note optional: empty note publish -> 200.
- PRECEDENCE PROOF: /api/v1/env/nope/config -> 404 application/json (no flag-table bytes); /nope-xyz -> 200 index HTML. QA channel curl+grep (playwright NOT installed — npx fetched npm wrapper only). Evidence: .omo/evidence/task-13-confignest.log (36/36).
- GOTCHA: server/confignest binary was STALE (pre-migration wiring) — first QA boot had zero confignest collections ("Missing or invalid collection context"); `go build -o confignest .` + restart fixed. Always rebuild the binary before serve-QA.

## 2026-09-22 task-14 Dart realtime SSE + poll fallback (port 8104)
- FILES (client/dart/ ONLY): lib/src/realtime.dart (new: RealtimeUpdater — SSE GET /api/v1/env/:env/stream + X-ConfigNest-Key, buffer-until-blank-line frame parse, `:` comments ignored, decoded {version,etag} on broadcast notifications, disconnect(); Timer.periodic(pollInterval, default 15min) ALWAYS runs incl. stream-healthy; reconnect backoff 1s,2s,4s…cap 30s; never throws — errors to status/lastError only), lib/src/config_nest_base.dart (connectRealtime({pollInterval}) + disconnectRealtime(), onUpdate emits snapshot ONLY when forced fetch returned true, fetchAndActivate gains force:false default to bypass 12h throttle on the realtime path, dispose closes updater first), lib/config_nest.dart (+realtime export), test/realtime_test.dart (4 tests, local dart:io HttpServer doubles + MockClient fetch).
- STALENESS BOUND (T17 contract): push best-effort ~instant while connected (measured 64ms localhost; NO sub-second guarantee claimed); otherwise <= pollInterval + one fetch. Poll runs in every state (belt+suspenders), so the bound holds stream-up, -down, and -401 alike.
- CONCURRENCY: event-refresh fire-and-forget, last-wins, no lock (single-threaded; same discipline as fetchAndActivate). onUpdate broadcast: late listeners miss snapshots; unchanged (304/throttled/error) refreshes emit nothing.
- RISKS: (1) Timer lifecycle — connect starts a periodic Timer + stream loop; forgetting disconnect/dispose leaks the isolate (tests dispose every client; suite wall ~1.8s proves no dangle). (2) Reconnect storm — spike stream is one-shot canned (emits once, closes) so a connected updater reconnects per backoff cadence; production stream must be long-lived or backoff traffic grows with subscriber count. (3) Spike keying — spike /stream hardcodes key "spike" + env allowlist {dev}, so a real SDK key ALWAYS 401s on stream (surfaced, not thrown); true publish→push fan-out does not exist server-side — freshness was proven via poll, push-parsing via the canned event.
- QA: analyze 0; dart test 18/18 (14 existing + chunk-split event→fetch→emit, dead-port poll fallback, malformed-frames-ignored + single emit, 401-surfaces-without-throw). Live 8104 (/tmp/cn-t14-pbdata): push event v1 observed in 64ms (spike key); prime v1 live_flag=false; publish v2 (flag PATCH + baseVersion=1, etag a16444ef2eb28b73); real-key poll=2s delivered onUpdate live_flag=true version=2 in 2s with stream=error/401 + post-settle status=cached (proves force bypassed default 12h throttle); dead-port 8105 poll=1s ticked despite refused stream (no throw/hang). Evidence: .omo/evidence/task-14-confignest.log. Cleanup: DOWN_CONFIRMED, /tmp/cn-t14* removed, no pb_data.
- GOTCHAS: (1) dart:io HttpResponse test doubles MUST set bufferOutput=false — with default buffering, write+flush delivers headers but ZERO body chunks until close (verified vs raw HttpClient); the Go server flushes correctly, so this is test-double-only. (2) _superusers API signup is 403-closed on v0.40.4 even on a fresh dir — provision via CLI `superuser upsert` then auth-with-password for the token.

## 2026-09-22 task-14fix stream productionize (port 8110) — STREAM CONTRACT CHANGE
- CONTRACT (server/spike_probe.go ONLY, spikeConfig stays deleted): GET /api/v1/env/:env/stream now uses REAL-key auth via ingest.RequireSDKKey reuse (import confignest/ingest) + env-scope check, order identical to fetch/ingest: 401 (key) -> 404 (unknown env slug) -> 401 (key env != env id). Hardcoded spike key + canned {dev} allowlist GONE from the stream path. After auth + SSE headers the handler holds long-lived: `: ping` keepalive comment every 20s until request-context done (client disconnect) or 10min cap — NO canned config_update event (avoids stale-event churn; freshness covered by the 15min poll fallback). Loop runs inline on the request goroutine (no per-conn goroutine, ticker+timer stopped via defer, write/flush errors return nil) so nothing leaks after close. POST /spike/publish stays hardcoded-spike-key and is now commented QA-ONLY hook, never production; no publish-triggered fan-out (out of scope).
- QA (fresh /tmp/cn-t14fix-pbdata, own key t14fix-qa-key-001-REAL prefix=t14fix-q): real-key stream 200 + Content-Type text/event-stream asserted in python (not just 200), time-to-first-ping=20.0s == interval; foreground curl --max-time 25 -> exit 28 (client bound) with exact 8-byte `: ping` body, config_update count=0; 70s background hold got 24 bytes = 3x pings, server never closed. Matrix: no-key/garbage/bad-key 401, dev-key-on-stage 401, unknown env 404, fetch 200 {version:0,etag:none} (empty-release policy), publish bad-key 401 / spike-key+bogus-client 404 / spike-key+live-subscribed-client 200 {delivered:true} with client observing 1x config_update. Evidence: .omo/evidence/task-14fix-confignest.log (28 lines). `go build/vet` green, `gofmt -l` empty, `go test ./...` all 5 pkgs green. gopls LSP still declined — vet+gofmt used instead.
- STRAY BINARY: server/confignest (34MB pre-migration QA build) removed via rm -f, `ls server/confignest` -> No such file. Same staleness class as the T13 gotcha — never QA from a checked-in binary, always rebuild.
- CLEANUP: server PID 56641 killed (lsof -ti:8110 empty + curl 000 DOWN_CONFIRMED), /tmp/cn-t14fix-{pbdata,confignest,serve.log,token,key,matrix.log,rt.log,rt2.log,cid,stream.hdr,stream.body} all removed (ls receipts), no pb_data in repo tree. No git (no repo).
- GOTCHA: this shell reports `$!`=0 after `(cmd &)` (PID capture unreliable) — PID-track via `lsof -ti:PORT` + `ps -o command` as authority, consistent with the T1 rule.

## 2026-09-22 task-15 retention purge (port 8105) — RETENTION SEMANTICS (T17/T18 read this)
- POLICY: raw `events` die at 30d, `event_daily` aggregates live to 90d. Cutoff rule is STRICTLY-OLDER-THAN (ts.Before(cutoff) deleted, exactly-equal KEPT, zero-ts KEPT — zero-ts has no day bucket so it is never purged; unit-proven in purge_test.go TestShouldDeleteCutoffEdges).
- ROLLUP-BEFORE-DELETE (same op, server/purge/purge.go PurgeOlderThan): doomed rows aggregate into event_daily upserts keyed (day,env,flag,variant) with day=UTC-midnight bucket BEFORE any delete; then stale event_daily rows older than cutoff-60d (=90d when cutoff=now-30d) die too. Returns total deleted (raw+stale rollups). Unknown kinds ignored, unset-flag rows roll up with flag unset.
- CRASH SEMANTIC: upsert-then-delete = no silent loss; a crash between them double-counts only the in-flight bucket on re-run (key stays one-row-per-key, counters can overshoot once). Clean re-run converges: second purge -> deleted:0, rollup bytes unchanged (proven live).
- ROUTE: `POST /api/v1/admin/maintenance/purge` superuser-only (SDK-key -> 401, unauth -> 401); ?dry= absent/0/false live, 1/true dry-run (zero writes, same count), anything else (abc) -> 400; empty table -> 200 deleted:0. Daily 24h process-local ticker (cutoff=now-30d); NO PB cron — single binary needs no scheduler dep. Releases/rollups collections never touched (events only). No GDPR export UI (deletion note in code comment only).
- QA (fresh /tmp/cn-t15-pbdata, T9 ts-override seed): 1x40d-old exposure + 5x fresh -> dry=1 twice same report deleted:1 zero mutation -> dry=0 deleted:1 -> 40d row GONE (sqlite userHash count 0), rollup `2026-08-13|control|0|1` exact, fresh 5 intact, stats7 {2,3,{control:2,treatment:1}}; regressions events-202/stats-200/fetch-200 + migrate up no-op. Evidence: .omo/evidence/task-15-confignest.log (PASS=38 FAIL=0).
- GOTCHAS: (1) port 8106 was squatted by a foreign process during QA (its /hello 200 + purge 404) — always ps-verify the PID owns the port before trusting responses; redid stale check on :8107 with ps receipt. (2) Same-package migration helpers collide — new file defines its own deny rules inline (no shared relation() reuse). (3) main.go wiring is 3 lines (import + purge.Register(se) after stats.Register(se)); T16 must NOT touch main.go. (4) `go test ./...` all 7 pkgs green, vet green, gofmt clean.

## 2026-09-22 task-16 load + security gate (port 8106, runner k6 v2.3.0)
- GATE (60s 100rps fetch + 50rps exposure, 9001 reqs, JSON-parsed asserts): fetch p95=0.782ms (<200ms budget, ~256x headroom), exposure p95=0.812ms, http_req_failed=0.00% (<1%). Gate key used rateLimit=100000 (established T11 pattern); fetch path is not token-metered (no limiter call in fetch.go) so 100rps fetch never 429s by design.
- BURST (200rps x 15s on DEFAULT-60 key, no rateLimit field): 3000 total = 60x202 + 2940x429 + 0x500 — exact default-60 fixed-window proof; zero-500 counters are omitted from k6 summary JSON (assert via http_reqs total = 202+429).
- LAG spot: 100-batch 202 -> countable in 636ms (single p50-ish sample, <=2s bound re-confirmed). MATRIX post-load: publish 200 (v2), fetch 304, stats 200 {fetches:160,exposures:3001,version:2}. RECOVER 30s@100rps post-burst: 3001x200, 0x429, 0x500 (no stuck state). WAL re-verified live twice (`pragma journal_mode` == wal).
- FILES: docs/SECURITY.md (key storage prefix+sha256, rotation incl. PB tokenKey-refresh invalidation of impersonate tokens source-verified in core/record_model.go:1452, userHash-only + T15 purge 30d/90d, 60/min limiter, CORS, superuser-only admin, stream 10min cap + poll bound, gate numbers) + scripts/k6-fetch.js (constant-arrival-rate 100/50, 60s hard bound, custom Trend thresholds fetch_duration p95<200 + http_req_failed<1%). Runner=k6 (brew-installed v2.3.0 for this gate); NO hey fallback shipped (k6 present).
- HEADERS: new server/security/ helper SetHeaders (nosniff + DENY + no-referrer) called from fetch getConfig/optionsConfig + releases postPublish/postRollback + stats getStats ONLY; main.go untouched (T15 already owns it now — purge import + Register landed mid-task, hands-off confirmed). Live-verified 200s on all three paths (401 middleware path keeps PB default SAMEORIGIN — middleware rejects before handler; accepted). go test ./... all pkgs green (incl. purge+security), vet green, gofmt clean.
- GOTCHAS: (1) bg server REAPED mid-task (graceful OnTerminate "batcher stopped" in serve.log) — relaunch with nohup+...+disown, NOT bare `(cmd &)`; (2) macOS has no `setsid`, and a failed `setsid` redirect TRUNCATED serve.log (redirection runs before exec) — k6 evidence in .omo log was intact; (3) k6 v2.3.0 summary-export nests values FLAT on the metric (no `values` key) and threshold booleans mean breached-or-not — assert on raw p(95)/rate numbers, not the booleans; (4) limiter is in-memory: restarts reset windows (window-reset proof N/A after relaunch; burst split remains the proof); (5) k6 constant-arrival-rate overshoots by ~1 iter (9001/3001/3001 totals) — assert n200==total, not ==rate*duration.
- RISKS: threshold headroom is huge on localhost (0.78ms vs 200ms) but this is loopback on one machine — no network/latency realism; burst key used fetch-kind events only (exposure path shares the same limiter call, same code); server serves from server/ cwd (T13 note still applies).

## 2026-09-22 F2 quality review (static, read-only)
- go1.27.0 installed matches go.mod `go 1.27` — no GOTOOLCHAIN download; build/vet/gofmt all clean, `dart analyze` 0 issues. gopls still uninstalled (declined); vet+gofmt remains the accepted proof.
- e.App discipline holds: all handlers re.App, hooks e.App, helpers take app param; only 2 `go` statements (batcher flusher, purge ticker) both on serve-scoped app with in-code docs — not leaks. Zero dbx imports in app code (indirect only); WAL via e.App.DB() in OnServe.
- TODO(todo6) markers survive ONLY in eval/vectors_test.go comments/error strings (impl green since task-6) — cosmetic rename candidate, not a stub. Sole `placeholder` hit is an HTML input attr. No _pb.js/jsvm/onRequest; no myapp/ prefix; server/confignest binary still absent.
- Migration header stale comment (line 10 said "") is FIXED — now reads default-deny (nil). Untracked server/pb_data/ runtime residue on disk — keep git-ignored.
- VERDICT: APPROVE (report: .omo/evidence/f2-quality-review.md).

## 2026-09-22 F4 scope fidelity (read-only audit, no code touched)
- RESULT: 7/7 capabilities PRESENT, 5/5 Must-NOT-haves ABSENT — VERDICT: APPROVE. Report: .omo/evidence/f4-scope-fidelity.md.
- Vectors count: `grep -c '{name:' server/eval/vectors_test.go` = 46 (>=40 required).
- Must-NOT grep receipts: significance/BigQuery/ClickHouse/RBAC/SSO/jsvm/validate_only zero implementation hits (only disclaimer server/eval/eval.go:50); oauth2 is indirect-only in server/go.mod:31 + go.sum (PocketBase transitive, never imported in .go).
- Boundaries kept: 1000 flags/project (main.go:31,86; snapshot.go:66), opaque etag no If-Match write path, superuser-only admin on all 4 routes, single binary (process-local ticker/limiter/writeMu).
- Tooling note: gopls LSP still not installed (user-declined); Grep + Read with file:line receipts substituted for lsp_symbols per task fallback.

## 2026-09-22 F1 plan compliance audit (read-only)
- VERDICT: APPROVE. Todos 1-18 all [x] with all six sections each (What-to-do/References/Acceptance/QA/evidence-path/Commit); evidence logs 18/18 on disk plus 8fix/14fix bonus; missing-todo list empty. Report: .omo/evidence/f1-compliance-audit.md.
- Contract intact: zero validate_only/409-retry/3000-params/BigQuery/JSVM in server/ + client/dart/lib/; 1000 flags/project + regex ^[A-Za-z_][A-Za-z0-9_]*$ enforced in main.go + snapshot.go; 10000-bps weights and pure-Dart file-JSON cache are documented deviations that supersede plan text, not violations.
- Stale-comment risk CLOSED: migrations header now says default-deny (nil). Git repo exists now; audit made no commits (read-only posture kept; tree has 2 modified + pb_data untracked, left untouched).

## 2026-09-22 F3 manual QA (port 8120) — VERDICT: APPROVE
- MATRIX (scratch /tmp/cn-f3-*, fresh build, evidence .omo/evidence/f3-manual-qa.log, 10 PASS / 0 product FAIL): publish v1 200 {v1,etag 934430719a3cfef3}; fetch 200 {version,etag,values launch_flag:true} via ios rule; If-None-Match exact -> 304 size 0, W/"..." weak -> 200; bad-key 401 PocketBase shape; stale baseVersion -> 409 {currentVersion:1} no write; burst 70x on default-60 key -> 60x202+10x429+0 other (exact fixed-window proof, burst used flag=burst_probe so launch_flag stats stayed exact); 100-batch 202 -> stats EXACT {fetches:40,exposures:60,perVariant:{control:30,treatment:30},version:1}; rollback v1->v2 {version:2,etag 4a51c7f3c64f497b}; sqlite snapshot bytes v1==v3-byte-identical (len 187) with differing etags matching served; fetch v2 values == v1; pragma table_info(events) has userHash + zero userId/ip cols; pragma journal_mode == wal.
- DART: analyze 0 issues; test 18/18; live example vs scratch v2 -> version=2 etag=rollback-etag success; dead-port+cache -> version=2/etag served status=error (offline proof); dead-port+no-cache -> version=0 defaults status=error exit 0 no throw (cold proof). Note: example fetches without platform=ios so launch_flag=false (default layering, expected).
- K6 (binary present v2.3.0, real gate, 60s): 9002 reqs, fetch p95=1.505ms (<200), exposure p95=1.491ms, failed=0.0000 (<1%) -> PASS. Task-16 numbers reproduced in class (p95 ~1.5ms vs 0.78ms prior; same orders, same 0% failed).
- SUITES: go test ./... -count=1 all pkgs ok; go vet clean; gofmt -l empty (gopls still declined, vet+gofmt stand in).
- GOTCHAS: (1) shell here is zsh — ${PIPESTATUS[0]} is EMPTY (bash-ism); assert on body content/exit codes instead. (2) k6 v2.3.0 summary-export: Rate metrics nest as {passes,fails,thresholds,value} — failure ratio is ["value"], NOT ["rate"]; Trend p(95) is flat. (3) curl-down `000000` concat restated: compare `*000*` or use curl exit 7 + lsof-empty as authority. (4) curl -o never creates the body file on 304 in this setup — size_download 0 is the emptiness proof. (5) Two log FAIL lines are harness artifacts (python SyntaxError batch builder before retry; 000000 concat), both corrected in-log with receipts — zero product failures.
- CLEANUP: lsof-8120 empty + curl exit 7 DOWN_CONFIRMED; /tmp/cn-f3-* removed (foreign /tmp/cn-f3-run.sh 18:07 left alone); server/pb_data (20:08, :8090 default serve) untouched; no source/migration edits, nothing committed.

## 2026-09-22 git surgery: single junk commit split into 18 per-todo commits
- RESET: `git reset --mixed HEAD~1` impossible (8c2080d was the root commit — HEAD~1 does not exist); used equivalent `git update-ref -d HEAD && git reset` (working tree kept, index emptied), then rebuilt 18 commits bottom-up. No push (forbidden), no source edits.
- SPLIT: 1 chore(module) 2 spike(spike_probe.go kept per T14fix — evolved into production stream path) 3 vectors 4 dart-scaffold 5 migrations+main.go 6 eval 7 experiment 8 releases 9 ingest 10 fetch 11 stats 12 dart-cache/base/events 13 pb_public 14 realtime 15 purge 16 security+k6+SECURITY.md 17 e2e+fixtures+e2e.sh(final state) 18 Makefile+CI+README+CONTRACT+pubspec.lock(T18 — lockfile belongs to release cut, not scaffold)+this notepad entry(-f, .omo/ is ignored).
- ANACHRONISM: server/main.go full wiring lands in T5 although later todos added registrations — unavoidable squashed-snapshot split (noted in T5 body).
- IGNORE: .gitignore hardened (server/pb_data/, client/dart/.dart_tool/, server/confignest); 4 tracked dart_tool artifacts (package_config/graph, snapshot, incremental kernel) dropped from index, left on disk ignored; server/pb_data/ stays untracked+ignored; .omo/evidence/*.log untouched and uncommitted (process artifacts, never in strategy).
